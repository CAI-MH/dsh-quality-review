/** Load SOP keywords from a directory tree (recursive, deduplicated). */
export declare function loadSopKeywords(dir: string): string[];
/** True when the user prompt contains any keyword (case-insensitive substring). */
export declare function matchesSop(userPrompt: string, keywords: string[]): boolean;
//# sourceMappingURL=sop.d.ts.map